# En développement, l'administration a son propre serveur Vite.
# En production, l'API sert les deux applications sur la même origine et la
# valeur par défaut « /admin/ » du code s'applique — ce fichier n'est pas utilisé.
VITE_URL_ADMIN=http://localhost:5174/admin/
