# Refuse l'installation si Node est trop ancien, avec un message explicite,
# plutôt que de laisser l'installation échouer plus loin de façon obscure.
# Electron exige Node >= 22.12 (voir « engines » dans package.json).
engine-strict=true
